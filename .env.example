# Supabase (issue #7) — copie de Project Settings › API no painel do Supabase.
# Sem estas variáveis o app ainda renderiza, mas nenhuma feature persiste dados.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Usado apenas por `npm run types:db` para regenerar lib/supabase/database.types.ts.
SUPABASE_PROJECT_ID=
